#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {

        int a, b, c;
        cin >> a >> b >> c;

        (a + b) == c ? cout << "YES" << endl : cout << "NO" << endl;
    }
    return 0;
}