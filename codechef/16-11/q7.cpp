#include <iostream>
#include <string>
using namespace std;
int main()
{
    int n;
    string s[n + 1];
    cin >> s;
    int C[26];
    for (int i = 0; i < 26; i++)
    {
        cin >> C[i];
    }

    int k;
    while (k--)
    {
        for (int i = 0; i <= k; i++)
        {
            string p[i + 1];
            cin >> p;
        }
    }

    return 0;
}