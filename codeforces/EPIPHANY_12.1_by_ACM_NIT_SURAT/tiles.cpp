#include <bits/stdc++.h>
using namespace std;

int main()
{
    int h, k, m, n;
    cin >> h >> k >> m >> n;

    // m tiles = 1*2
    // n tiles = 2*1

    // we will use a recursive function 
    
    return 0;
}